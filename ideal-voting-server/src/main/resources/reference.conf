dbTransactor {
  user = "idealvoting"
  password = "idealvoting"
  url = "jdbc:mysql://localhost:3306/idealvoting"
  threadPoolSize = 32
  driverClassName = "org.mariadb.jdbc.Driver"
  liquibaseChangeLogFile = "db/changelog/db.changelog-master.yaml"
}
httpServer {
  host = "localhost"
  port = 8080
}
